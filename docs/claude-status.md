# Claude 状态与提醒

并行工作区的创建、恢复入口及对应后端操作已移除。已有远程 worktree、分支、tmux 会话和任务文件不会被删除或终止。

任务状态面板和桌面提醒继续保留。已由旧版接入 Hooks 的 Claude 任务仍可通过 SSH 只读跟踪状态，并从任务列表打开现有 tmux 终端；普通终端继续使用通知协议和带“推测”标记的输出识别。

本轮结束不代表整个任务完成，SSH 断线期间无法获取最新状态。没有安装 Hooks 的普通 Claude 会话不会自动获得精确事件状态。
