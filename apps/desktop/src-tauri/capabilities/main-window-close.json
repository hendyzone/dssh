{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window-close",
  "description": "Allow the main window to complete a user-requested close after session confirmation.",
  "windows": ["main"],
  "permissions": ["core:window:allow-destroy"]
}
