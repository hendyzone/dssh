import {render,screen,fireEvent,waitFor} from "@testing-library/react";import {it,expect,vi} from "vitest";import {invoke} from "../src/platform/core";import ChangesPanel from "../src/components/ChangesPanel";
vi.mock("../src/platform/core",()=>({invoke:vi.fn()}));
it("separates staged and working diffs and passes literal paths",async()=>{vi.mocked(invoke).mockImplementation(async(command)=>command==="workspace_changes"?{root:"/repo",branch:"main",files:[{path:"a b.ts",index:"M",worktree:"M"}]}:"+changed");render(<ChangesPanel sessionId="session" cwd="/repo" onClose={()=>{}}/>);await screen.findByText("未暂存 · 1");const files=screen.getAllByRole("button",{name:/M a b.ts/});fireEvent.click(files[1]);await waitFor(()=>expect(invoke).toHaveBeenCalledWith("workspace_diff",{sessionId:"session",root:"/repo",path:"a b.ts",kind:"staged"}));expect(await screen.findByText("+changed")).toBeTruthy();});
